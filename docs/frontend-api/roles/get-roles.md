# Frontend API: Get Roles (`get-roles.md`)

- **HTTP Method & URL**: `GET /api/v1/roles`
- **Headers**: `Authorization: Bearer <accessToken>`
- **Response (`200 OK`)**: Returns list of all system and custom role definitions in the tenant.
