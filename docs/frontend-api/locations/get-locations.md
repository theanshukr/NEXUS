# Frontend API: Get Locations (`get-locations.md`)

- **HTTP Method & URL**: `GET /api/v1/locations`
- **Headers**: `Authorization: Bearer <accessToken>`
- **Response (`200 OK`)**: Returns array of office locations.
