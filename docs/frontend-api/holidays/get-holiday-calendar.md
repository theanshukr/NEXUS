# Frontend API: Get Holiday Calendar (`get-holiday-calendar.md`)

- **HTTP Method & URL**: `GET /api/v1/locations/:locationId/holidays/:year`
- **Headers**: `Authorization: Bearer <accessToken>`
- **Response (`200 OK`)**: Returns holiday calendar array for the location and year.
