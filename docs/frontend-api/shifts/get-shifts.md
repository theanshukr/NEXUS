# Frontend API: Get Shifts (`get-shifts.md`)

- **HTTP Method & URL**: `GET /api/v1/shifts`
- **Headers**: `Authorization: Bearer <accessToken>`
- **Response (`200 OK`)**: Returns array of shift schedules.
