# Frontend API: Get Employees (`get-employees.md`)

- **HTTP Method & URL**: `GET /api/v1/employees`
- **Headers**: `Authorization: Bearer <accessToken>`
- **Query Parameters**: `status`, `departmentId`, `search`, `includeArchived`, `onlyArchived`
- **Response (`200 OK`)**: Returns workforce directory list.
